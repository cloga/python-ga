GA report API的一个Python简单实现
Python_GA.py
支持指定proxy

get_toke：获得token
args：定义查询条件
get_data(args)：获得基本数据，返回的为Dict结构的数据。
get_mcf_data(args)：获得MCF的数据，返回的为Dict结构的数据。
write_data(headers,rows,name='ga_data')：将基本数据输出为CSV。
write_funnel_data(headers,rows,name='funnel_data')：输出多渠道数据。
write_mcf_data(headers,rows,name='mcf_data')：输出MCF的数据，包含First Touch，Last Touch和Linear三种模型
