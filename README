GA report API的一个Python简单实现
Python_GA.py
支持指定proxy

get_toke：获得token
args：定义查询条件
get_data(args,type='ga')：获得数据，返回的为Dic结构的数据。type指定查询的数据类型，ga代表常规的报告数据，mcf为多渠道漏斗的数据
write_csv(data,type='ga',name='ga_data')：data是get_data返回的数据；type为数据类型，ga代表常规的报告数据，mcf为多渠道漏斗的数据；name是文件名，默认为ga_data
